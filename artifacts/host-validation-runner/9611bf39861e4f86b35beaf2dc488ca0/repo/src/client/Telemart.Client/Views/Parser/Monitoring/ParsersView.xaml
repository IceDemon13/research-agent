<UserControl x:Class="Telemart.Client.Views.Parser.Monitoring.ParsersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:dxmvvm="http://schemas.devexpress.com/winfx/2008/xaml/mvvm"
             xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core"
             xmlns:dxb="http://schemas.devexpress.com/winfx/2008/xaml/bars"
             xmlns:viewModels="clr-namespace:Telemart.Client.ViewModels.Parser.Monitoring"
             xmlns:client="clr-namespace:Telemart.Client"
             xmlns:controls="clr-namespace:Telemart.Client.Controls"
             DataContext="{client:Container Type=viewModels:ParsersViewModel}"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance Type={x:Type viewModels:ParsersViewModel}, IsDesignTimeCreatable=True}">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Telemart.Client;component/Style/MainStyle.xaml" />
                <ResourceDictionary></ResourceDictionary>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <dxb:ToolBarControl Grid.Row="0"
                            Height="Auto"
                            AllowQuickCustomization="False"
                            AllowCustomizationMenu="False"
                            BorderThickness="1">
            <dxb:BarButtonItem Content="Назад"
                               LargeGlyph="{dx:DXImage Image=Prev_32x32.png}"
                               Glyph="{dx:DXImage Image=Prev_16x16.png}"
                               ItemClick="PrevClick"/>
            <dxb:BarButtonItem Content="Вперед"
                               LargeGlyph="{dx:DXImage Image=Next_32x32.png}"
                               Glyph="{dx:DXImage Image=Next_16x16.png}"
                               ItemClick="NextClick"/>
            <dxb:BarItemLinkSeparator></dxb:BarItemLinkSeparator>
            <dxb:BarButtonItem Content="Обновить"
                               LargeGlyph="{dx:DXImage Image=Refresh_32x32.png}"
                               Glyph="{dx:DXImage Image=Refresh_16x16.png}"
                               ItemClick="Refresh"/>
            <dxb:BarItemLinkSeparator/>
            <controls:WikiBarButtonItem />
        </dxb:ToolBarControl>
        <WebBrowser Grid.Row="1"
                    x:Name="WebBrowser"
                    Navigating="WebBrowserOnNavigating"
                     Navigated="WebBrowserOnNavigated" >
            <dxmvvm:Interaction.Behaviors>
                <dxmvvm:DependencyPropertyBehavior PropertyName="Source" Binding="{Binding Path=Url, Mode=OneWay}"/>
            </dxmvvm:Interaction.Behaviors>
        </WebBrowser>
    </Grid>

</UserControl>
