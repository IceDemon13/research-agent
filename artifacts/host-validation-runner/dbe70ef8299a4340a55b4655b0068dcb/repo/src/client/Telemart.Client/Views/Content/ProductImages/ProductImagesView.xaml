<UserControl x:Class="Telemart.Client.Views.Content.ProductImages.ProductImagesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:dxb="http://schemas.devexpress.com/winfx/2008/xaml/bars"
             xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core"
             xmlns:dxg="http://schemas.devexpress.com/winfx/2008/xaml/grid"
             xmlns:dxmvvm="http://schemas.devexpress.com/winfx/2008/xaml/mvvm"
             xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors"
             xmlns:dxlc="http://schemas.devexpress.com/winfx/2008/xaml/layoutcontrol"
             xmlns:controls="clr-namespace:Telemart.Client.Controls"
             xmlns:client="clr-namespace:Telemart.Client"
             xmlns:behaviors="clr-namespace:Telemart.Client.Common.Behaviors"
             xmlns:viewModels="clr-namespace:Telemart.Client.ViewModels.Content.ProductImages"
             DataContext="{client:Container Type=viewModels:ProductImagesViewModel}"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=viewModels:ProductImagesViewModel, IsDesignTimeCreatable=True}"
             d:DesignWidth="1024"
             d:DesignHeight="768">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Telemart.Client;component/Style/MainStyle.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <dxmvvm:Interaction.Behaviors>
        <dxmvvm:FolderBrowserDialogService />
        <dxmvvm:EventToCommand EventName="Loaded"
                               Command="{Binding Path=HandleLoadedCommand, Mode=OneTime}" />
    </dxmvvm:Interaction.Behaviors>

    <dxlc:LayoutControl x:Name="LayoutControl"
                        Orientation="Vertical"
                        Padding="0"
                        ItemSpace="0">
        <dxb:BarManager VerticalAlignment="Top">
            <dxb:ToolBarControl IsEnabled="{Binding Path=ShowLoadingPanel, Mode=OneWay, ElementName=GridControl, Converter={dxmvvm:BooleanNegationConverter}}">
                <dxb:BarButtonItem Content="Очистить"
                                   LargeGlyph="{dx:DXImage Image=New_32x32.png}"
                                   Glyph="{dx:DXImage Image=New_16x16.png}"
                                   Command="{Binding Path=ClearCommand, Mode=OneTime}" />
                <dxb:BarButtonItem Content="Добавить"
                                   ToolTip="Добавить (Insert)"
                                   LargeGlyph="{dx:DXImage Image=Add_32x32.png}"
                                   Glyph="{dx:DXImage Image=Add_16x16.png}"
                                   Command="{Binding Path=AddCommand, Mode=OneTime}" />
                <dxb:BarButtonItem Content="Удалить"
                                   ToolTip="Удалить (Del)"
                                   LargeGlyph="{dx:DXImage Image=Delete_32x32.png}"
                                   Glyph="{dx:DXImage Image=Delete_16x16.png}"
                                   Command="{Binding Path=DeleteCommand, Mode=OneWay}" />
                <dxb:BarItemSeparator />
                <dxb:BarButtonItem Content="Сохранить"
                                   LargeGlyph="{dx:DXImage Image=Save_32x32.png}"
                                   Glyph="{dx:DXImage Image=Save_16x16.png}"
                                   Command="{Binding Path=SaveCommand, Mode=OneWay}" />
                <dxb:BarItemSeparator />
                <controls:WikiBarButtonItem />
            </dxb:ToolBarControl>
        </dxb:BarManager>
        <dxlc:LayerSeparator Height="1"
                             Kind="Horizontal" />
        <dxg:GridControl x:Name="GridControl"
                         CurrentItem="{Binding Path=SelectedProductDirectoryItem, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                         ItemsSource="{Binding Path=ProductDirectoryItems, Mode=OneWay}"
                         ShowBorder="False"
                         SelectionMode="None"
                         ShowLoadingPanel="{Binding Path=AddCommand.IsExecuting, Mode=OneWay}"
                         DesignTimeDataObjectType="{x:Type viewModels:ProductImageDirectoryViewItem}">
            <dxmvvm:Interaction.Behaviors>
                <behaviors:LayoutSerializationBehavior FileName="product_images.xml" />
                <behaviors:CopyCurrentCellToClipboardBehavior />
            </dxmvvm:Interaction.Behaviors>
            <dxg:GridControl.Columns>
                <dxg:GridColumn Header="Путь к папке"
                                HeaderToolTip="Путь к папке"
                                FieldName="Path"
                                Width="0.3*" />
                <dxg:GridColumn Header="Папка"
                                HeaderToolTip="Папка"
                                FieldName="DirectoryName"
                                Width="0.35*">
                </dxg:GridColumn>
                <dxg:GridColumn Header="Товар"
                                HeaderToolTip="{Binding Path=Header, Mode=OneWay, RelativeSource={RelativeSource Self}}"
                                FieldName="ProductName"
                                Width="0.35*">
                    <dxg:GridColumn.CellTemplate>
                        <DataTemplate>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <dxe:TextEdit x:Name="PART_Editor" />
                                <dx:SimpleButton Grid.Column="1"
                                                 Command="{Binding Path=DataContext.SelectProductCommand, Mode=OneWay, ElementName=LayoutControl}"
                                                 Content="..."
                                                 Width="20"
                                                 MinWidth="20"
                                                 Height="20"
                                                 Padding="1" />
                            </Grid>
                        </DataTemplate>
                    </dxg:GridColumn.CellTemplate>
                </dxg:GridColumn>
                <dxg:GridColumn Header="C"
                                HeaderToolTip="Статус"
                                FieldName="IsProcessed"
                                HorizontalHeaderContentAlignment="Center"
                                Width="24"
                                FixedWidth="True"
                                DisplayTemplate="{StaticResource BooleanTickColumnDisplayTemplate}" />
            </dxg:GridControl.Columns>
            <dxg:GridControl.View>
                <dxg:TableView AutoWidth="True"
                               AllowEditing="False"
                               NavigationStyle="Cell"
                               ShowIndicator="False"
                               ShowAutoFilterRow="False"
                               ShowFixedTotalSummary="False"
                               IsTotalSummaryMenuEnabled="False"
                               ShowTotalSummary="False"
                               ShowGroupPanel="False"
                               AllowGrouping="False"
                               AllowMoveColumnToDropArea="False"
                               AllowColumnMoving="False"
                               AllowColumnFiltering="False"
                               AllowSorting="True"
                               AllowResizing="True" />
            </dxg:GridControl.View>
        </dxg:GridControl>
    </dxlc:LayoutControl>

</UserControl>

