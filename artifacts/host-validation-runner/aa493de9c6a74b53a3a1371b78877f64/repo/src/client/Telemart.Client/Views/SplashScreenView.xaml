<UserControl x:Class="Telemart.Client.Views.SplashScreenView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core"
             mc:Ignorable="d" 
             d:DataContext="{x:Static dx:SplashScreenViewModel.DesignTimeData}">

    <Grid x:Name="LayoutRoot">
        <Grid x:Name="Splash" 
              HorizontalAlignment="Center" 
              VerticalAlignment="Top">
            <Grid x:Name="Back">
                <Border Background="Black" CornerRadius="3" Opacity="0.15"/>
                <Border Margin="1" Background="White"/>
            </Grid>
            <Grid x:Name="ContentArea" Margin="8">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <dx:DXImage Grid.Row="0" x:Name="Image" Source="../Images/Image.png" Width="320" Height="140"/>
                <ProgressBar Grid.Row="1" x:Name="ProgressBar"
                             Height="16"
                             IsIndeterminate="{Binding IsIndeterminate}"
                             Value="{Binding Progress}"
                             Maximum="{Binding MaxProgress}"/>

            </Grid>
        </Grid>
    </Grid>
</UserControl>
